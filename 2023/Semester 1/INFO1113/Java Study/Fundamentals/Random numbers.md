
```java
import java.util.Random; // random module

Random random = new random(); // create random instance object ( sudo random numbers )

int x = random.nextInt();

/* 
to limit the size of the number, and have a number between 1-6

int x = random.nextInt(6) + 1;
*/ 





```
